#version 460 core

in vec2 TexCoords;

out vec4 color;

uniform sampler2D imageTexture[16];
uniform int textureIndex;

void main(){
    color = texture(imageTexture[textureIndex], TexCoords);
    
    }